import 'package:flutter/material.dart';

class TicketPageBody extends StatelessWidget {
  const TicketPageBody({super.key});
  // design here
  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("Vacation page")]);
  }
}
